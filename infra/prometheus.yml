global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: querylens-backend
    static_configs:
      - targets: ["backend:8000"]
    metrics_path: /metrics
