# README Improvement Checklist

Use this list to keep the repository presentation honest and production-grade.

- Add exact local preview URLs for backend, frontend, Prometheus, and Grafana.
- Link the canonical benchmark summary and explain that it is synthetic local telemetry.
- Surface the evaluation proof tables for regression detection, placement simulation, and investigator quality.
- State clearly that placement is a what-if simulator, not live Azure cluster control.
- Add an implemented-versus-not-implemented table.
- Include screenshots or GIFs for landing, dashboard, query detail, regressions, placement, and reports.
- Show the local demo command in one place.
- Document the data flow from PostgreSQL to collector to Kafka to backend to UI.
- Call out the regression classes and diagnostic classes that are actually implemented.
- Mention the current limitations, especially around real Azure deployment claims.

